<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- Throwaway Phase 0 spike (S5). Deliberately outside the product reactor. -->
    <groupId>io.github.dinamo541</groupId>
    <artifactId>idearm-spike-editor</artifactId>
    <version>0.0.0-SNAPSHOT</version>
    <name>IDEARM spike S5 - editor</name>
    <description>RichTextFX performance with a large real Assembly file.</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>25</maven.compiler.release>
        <javafx.version>25.0.2</javafx.version>
        <!-- 0.11.5 fails on JavaFX 25 (TextFlowExt overrides the now-final TextFlow.getUnderlineShape); 0.11.7 fixes it -->
        <richtextfx.version>0.11.7</richtextfx.version>
        <!-- Pass the file to open: mvn javafx:run -Dspike.file=<path to loadmap.asm> -->
        <spike.file></spike.file>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>org.fxmisc.richtext</groupId>
            <artifactId>richtextfx</artifactId>
            <version>${richtextfx.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.14.1</version>
            </plugin>
            <plugin>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-maven-plugin</artifactId>
                <version>0.0.8</version>
                <configuration>
                    <mainClass>io.github.dinamo541.idearm.spikes.editor.EditorSpike</mainClass>
                    <commandlineArgs>${spike.file}</commandlineArgs>
                    <options>
                        <option>--enable-native-access=javafx.graphics</option>
                    </options>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
