<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>io.github.dinamo541</groupId>
        <artifactId>idearm-parent</artifactId>
        <version>1.0.0</version>
    </parent>
    <artifactId>idearm-toolchain-dos</artifactId>
    <name>IDEARM DOS Toolchain</name>
    <description>L4 Infrastructure: Borland adapters, diagnostics and isolated DOSBox builds.</description>
    <dependencies>
        <dependency>
            <groupId>io.github.dinamo541</groupId>
            <artifactId>idearm-domain</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
    <build>
        <testResources>
            <testResource><directory>${project.basedir}/../fixtures</directory><targetPath>fixtures</targetPath></testResource>
        </testResources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <executions>
                    <execution>
                        <id>dos-without-javafx</id>
                        <goals><goal>enforce</goal></goals>
                        <configuration><rules><bannedDependencies>
                            <excludes><exclude>org.openjfx:*</exclude></excludes>
                            <message>DOS toolchain infrastructure must not depend on JavaFX (ADR-007).</message>
                        </bannedDependencies></rules></configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
